            }
                arr[i] = arr[i-1];
                arr[i] = nums[i];
            } else {
            if(nums[i] >= arr[i-1]){
        for(int i = 1 ; i < n ; i++){
        }

        int min = Integer.MAX_VALUE;
        int ans = -1;

        for(int j = n - 1 ; j >= 0 ; j--){
            min = Math.min(min , nums[j]);
