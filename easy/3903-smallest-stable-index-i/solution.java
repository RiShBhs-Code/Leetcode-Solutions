
        int min = Integer.MAX_VALUE;
        int ans = -1;

        for(int j = n - 1 ; j >= 0 ; j--){
            min = Math.min(min , nums[j]);

        System.out.println(Arrays.toString(arr));
        }
            }
                arr[i] = arr[i-1];
            } else {
                arr[i] = nums[i];
