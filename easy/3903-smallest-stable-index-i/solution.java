            }
                arr[i] = arr[i-1];
                arr[i] = nums[i];
            } else {
            if(nums[i] >= arr[i-1]){

        for(int i = 1 ; i < n ; i++){
        arr[0] = nums[0];
        int[] arr = new int[n];
        int n = nums.length;

    public int firstStableIndex(int[] nums, int k) {
class Solution {
